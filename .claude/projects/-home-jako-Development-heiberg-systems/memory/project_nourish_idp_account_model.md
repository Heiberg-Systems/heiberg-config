---
name: project_nourish_idp_account_model
description: Nourish now sources identity/membership/subscription/profile from the IdP (account-model integration) — nourish
metadata: 
  node_type: memory
  type: project
  originSessionId: 6d344137-82fd-4e91-b29e-e5fa26b2f044
---

**2026-06-27.** Implemented "**persons from the IdP**" for Nourish (the principle Jako stated). **Nourish account-model integration → nourish #34 MERGED** (branch `feature/account-model-integration`, off develop). Decision (Jako): **every household member is a Keycloak user** (no login-less dependents); roster = the IdP account's active members. Plan `nourish/docs/plans/2026-06-27-account-model-integration-idp-native.md` (supersedes the 2026-06-18 plan for identity+membership+**profile**; production-data ETL deferred to cutover). Built via subagent-driven TDD (9 tasks + a residual router sweep).

**What it does:** identity/account/subscription from the JWT (`account_id`/`account_role`/`realm_access.roles`); `get_current_person` JWT-driven (no household join, **keeps `keycloak_id` so the Aide caller still works**); `require_active_subscription` gates on the `nourish:subscriber` realm role; roster pulled from portal-api `POST /internal/memberships/sync` (`portal_client.fetch_account_member_subs`) joined to per-member targets in the now-thin `persons` (keyed by `keycloak_id`); profile (gender/DOB/height/medical) read from `/internal/profile/{sub}` (`get_profile`) — `onboarding`/`persons` CRUD are **target-only** (profile editing is the IdP's `PATCH /api/profile/me`). **Retired routers** (IdP-owned): `households`, `subscriptions`, `medical_conditions`. **Dev migration 044** dropped `households`/`household_members`/`subscription_status`/`medical_conditions`/`subscription_plans` + the persons profile columns. portal-api is on `heiberg_net` at `http://portal-api:5089`; `/internal/*` guarded by `X-Internal-Secret` (`INTERNAL_SECRET`).

**Verified:** full Docker suite **428 passed / 0 failed / 100% on all 9 gated modules**. Whole-branch opus review caught 3 real issues the green suite missed (all FIXED): CRITICAL `health_snapshots` upload 500'd (`asyncio.run` in a running loop — Task-4 sync-helper rewrite broke the one async caller); IDOR in `calculate_targets` (write-by-`person_name` → roster-scoped); committed dev DB passwords. **Live e2e PROVEN**: generation as seeded Jako ran the full pipeline via Aide with the roster = Jako+Elize from the IdP account (analyst computed Elize's seeded 1400-1600 target). Non-persist = analyst gave up after 2 iters over Elize's tight target = pre-existing pipeline tuning (`MEAL_AI_MAX_ITER=2`), not integration.

**Leaked dev DB passwords — ROTATED ✅ (2026-06-28).** commit `48f52bc` had put the REAL `PORTAL_DB_PASSWORD` + `NOURISH_DB_PASSWORD` in git history (removed from working tree by `8222d6e`). Rotated: new 32-char alphanumeric passwords → `ALTER ROLE portal_user/nourish_user` + root `.env` updated + portal-api/nourish-backend recreated & reconnected. Verified the OLD passwords are now **rejected over the scram path** (`pg_hba`: `host all all all scram-sha-256` is the app path; `local`/`127.0.0.1` are `trust`, which is why a localhost test misleadingly accepted the old pw). The history copies are now dead credentials. **History ALSO scrubbed (2026-06-28):** `git filter-branch` over `d20d4c5..HEAD` (branch-only; develop untouched) replaced the two literals in `seed_idp_demo.py` across all commits → force-pushed; branch tip is now `ca5fbba` (was `8a22248`); final tree byte-identical (only history changed); remote + local history verified clean. (GitHub may briefly retain the old force-pushed commits by SHA until its own GC, but the passwords are rotated/dead so that's moot.)

**Dev demo state (left running):** IdP profile up (keycloak + portal-api), portal seeded (account `11111111-1111-1111-1111-111111111111`, Jako sub `93d3f1df-5d9b-44cf-becc-c05c7ada27f0` admin + Elize sub `996cd98f-...` member), nourish persons seeded, `nourish-backend` recreated on the new image, root `.env` has `DEV_AUTH_BYPASS=true` + `DEV_BYPASS_USER_ID`=Jako + `DEV_BYPASS_ACCOUNT_ID`=the account + `INTERNAL_SECRET=dev_internal_secret_2026` + `AIDE_KEYS`/`AIDE_SERVICE_KEY`. Re-seed: `app/backend/.venv/bin/python scripts/seed_idp_demo.py` (needs `KEYCLOAK_ADMIN_PASSWORD`). NOTE: portal DB account tables had to be created from `persistence/.../portal/portal.sql` + GRANT to portal_user (portal-api create_all only made the old tables).

**CLEANUP DONE (2026-06-28):** root #101 merged (nourish→41e7550 + persistence→e68327e pointers + planning docs Account-model ✅); persistence #14 merged (the stray direct-develop commit moved to a branch first); all feature branches deleted; root develop clean. **Deferred follow-ups (not blockers):** production-data ETL (cutover); multi-account/`X-Account-Id`/`viewer`; bump `MEAL_AI_MAX_ITER` for convergence; persistence submodule is detached at the pinned `e68327e` (develop tip is the #14 merge `f23ec7d` — 1-commit cosmetic gap, resolves on next bump).

Related: [[project_aide_gateway_tool_calling_prereq]] (the Aide migration this builds on — Aide caller uses `keycloak_id`), [[project_nourish_multitenant_purity]] (roster groundwork).

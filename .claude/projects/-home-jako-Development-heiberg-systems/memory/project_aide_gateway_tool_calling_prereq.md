---
name: project_aide_gateway_tool_calling_prereq
description: "Aide gateway sync tool-calling prereq built+PR'd (aide"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6d344137-82fd-4e91-b29e-e5fa26b2f044
---

**2026-06-26.** Starting Aide **Phase 6** (Nourish→Aide migration, plan: root `docs/plans/2026-06-23-aide-phase6-nourish-migration.md`) surfaced a **blocking gateway gap**: the plan (written 2026-06-23, pre-dating the real Aide impl) assumes `/v1/tasks` + `/v1/chat/completions` round-trip forced **tool calls**, but Aide's non-streaming `provider.complete()` **discarded** them — Anthropic kept only `text` blocks, OpenRouter read only `message.content`; `UnifiedResponse` had no `tool_calls`; `UnifiedRequest` had no `tool_choice`/`max_tokens`/`temperature` (Anthropic `max_tokens` hardcoded 16000); no prompt-caching pass-through. Tool calls only surfaced via the streaming conversation engine, not the gateway. Jako chose **"extend Aide first"** (the gap blocks every app's structured-output migration, not just Nourish).

**Built the prereq** (branch `feature/gateway-sync-tools-and-params`, **aide PR #6 → develop, OPEN**): non-streaming `/v1/tasks` + `/v1/chat/completions` now honour `tool_choice`/`max_tokens`/`temperature`/`cache_system` and surface OpenAI-shape `tool_calls` (`arguments` a JSON string); Anthropic adapter translates tool_choice + extracts `tool_use`→tool_calls + `cache_system`→`cache_control:ephemeral` + OpenAI `image_url`→Anthropic image blocks (vision); OpenRouter forwards params + returns `message.tool_calls`; **SDK `aide-client` 0.1.0→0.2.0** (`.task()`/`.chat()` forward the new fields). Built via subagent-driven TDD (8 tasks); whole-branch opus review = Ready-to-merge, no Critical/Important; backend **221** + SDK **8** green. Plan: aide `docs/plans/2026-06-26-gateway-sync-tools-and-params.md`. SDD ledger at aide `.superpowers/sdd/progress.md`.

**Aide #6 MERGED (2026-06-27).** **Nourish Phase 6 migration MERGED (nourish #32, develop tip d20d4c5).** Key decision (Jako): apps talk to Aide over its **HTTP API directly** (httpx) — **NO `aide_client` SDK install / no vendoring** (sidesteps the cross-repo packaging problem). The "Heibi" conversation popup is a separate frontend feature (Aide already has the conversation API + `@heiberg/aide-react`), layer-on later. Built via subagent-driven TDD (10 tasks, own plan `nourish/docs/plans/2026-06-27-aide-migration-direct-http.md` which SUPERSEDES the root SDK plan). `routers/ai/aide_gateway.py` is the only transport (POST `/v1/tasks`, 3 headers, Anthropic-tool→OpenAI-tools+forced-tool_choice, parses `tool_calls`). Reconciliations applied: `ai_targets.py` at `routers/ai_targets.py`; **`get_current_person` SELECT now adds `p.keycloak_id`** (it did NOT carry the sub before — Tasks 6/8 needed it); model pins `anthropic/claude-{haiku-4-5,sonnet-4-6,opus-4-8}` (route to direct adapter). Full Docker suite **469 passed / 1 pre-existing-env fail** (`test_subscriptions` webhook — PAYSTACK_SECRET_KEY set); coverage 99.52% (gaps pre-existing in nutrition_math+subscriptions, branch-untouched; gated auth+ai_targets=100%). Opus whole-branch review = Ready-to-merge; caught + fixed 2 Minors: a REAL frontend cost-ticker regression (per-stage SSE reverted to `cost_usd` dollars consistently) + fail-open→fail-closed caller guard.

**COMPLETE (2026-06-27): the Aide internal build (Phases 1–6) is done.** aide #6 MERGED, **nourish #32 MERGED** (develop tip d20d4c5), **root #100 OPEN** (bumps aide→e6b10ca + nourish→d20d4c5 + reconciles roadmap/status → Phase 4.5 ✅ / Phase 6 ✅). Merged local feature branches deleted in both submodules.

**Live Aide smoke RUN + PASSED (2026-06-27).** Drove Nourish's real `aide_gateway` (new image) against the live Aide stack → Anthropic **direct adapter**: forced-tool round-trip returned a validated dict + `provider:anthropic` + `cost_cents>0`; free-text round-trip OK; `tally_cost.py` (`/v1/usage`) showed non-zero `app=nourish` spend matching the ledger. Dev stack cut over (nourish backend recreated on the migrated image — `anthropic` pkg absent). Config added to root `.env` (persistent): `AIDE_KEYS=nourish:nrsh_smoke_key_2026` + `AIDE_SERVICE_KEY=nrsh_smoke_key_2026`; `aide` profile brought up. **NOT done (DB gap, not migration):** the full HTTP `/api/ai/generate-meal-plan` pipeline run — the dev nourish DB has **0 persons** (only 1 household, 6 meal_plans, 24 recipes); needs seeding a person + household_member + active subscription to run the real pipeline. **Minor (dev-only):** `ANTHROPIC_API_KEY` still injected into nourish via the shared-root `env_file` (Aide needs it there) but the `anthropic` pkg is gone so it's unusable; prod K8s scopes secrets per-service.

**RESUME (remaining, non-blocking):**
2. Still owed at the root (separate later task): shared-cluster `persistence/.../aide.sql` (Aide still runs its own dev postgres).
3. Natural next Nourish↔Aide step: the deferred "prompt/agent-definition layer → Aide hosted agent" (move PLANNER/CHEF prompts into an Aide agent — see [[project_nourish_multitenant_purity]]).
4. Non-blocking: aide still sends the legacy `anthropic-beta: prompt-caching-2024-07-31` header (harmless); validate the `cache_system` single-block cache discount vs real cache-read metrics. A scratch venv at `nourish/app/backend/.venv` (gitignored) remains for fast nourish backend TDD.

Related: [[project_aide_phase5]] (prior phase; also notes the later shared-cluster `persistence/.../aide.sql` root task), [[project_nourish_multitenant_purity]] (Nourish prompt/agent-definition layer also deferred to this migration).

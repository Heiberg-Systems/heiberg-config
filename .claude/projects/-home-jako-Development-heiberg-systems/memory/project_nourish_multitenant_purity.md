---
name: project-nourish-multitenant-purity
description: Nourish multitenant-purity (Tasks 1-8) — PR #31 merged; still-owed backend bits (nutrition_usda, weeks.py multipliers, workouts scoping) MERGED nourish #35 (2026-07-02); ONLY the planner parser remains, blocked-on-Aide
metadata: 
  node_type: memory
  type: project
  originSessionId: 6661e3f1-a0b2-4067-9483-658d8e8c502d
---

**UPDATE 2026-07-02 (nourish #35):** the still-owed backend bits are DONE — `usda._calculate_recipe_nutrition` made roster-driven (person-keyed `*_nutrition_usda`), `weeks.py` variant multipliers person-keyed, and `workouts` reframed to roster-scoping (which also **fixed a cross-account `training_programs` leak** in `list_workouts`). 441 tests pass, 13 new TDD. **ONLY the planner parser remains owed, blocked-on-Aide** (`PLANNER_TOOL_SCHEMA` in `routers/ai/models.py` still hardcodes Jako/Elize slot keys + `constants.py` `_PROTEIN_FLOORS` is 2-person → migrates as a unit when the schema moves to the Aide hosted agent). Root #130 reconciled the planning docs + bumped the pointer. Everything below is historical. [[project_pwa_standard_rollout]]

Nourish Phase 5 → "Remove hardcoded Jako/Elize" plan (`nourish/docs/plans/2026-06-09-multitenant-purity-and-shared-infrastructure.md`, roadmap "Tasks 1-8"). **Started 2026-06-26.**

**MERGED 2026-06-26 night:** **PR #31 MERGED** into nourish `develop` (merge `e710d66`); feature branch auto-deleted; local develop in sync. All Tasks 1-8 (partial) now on develop. Every consumer reads both formats via `_person_quantity` legacy fallback, so un-migrated live mongo keeps working and `mongo_001_quantities_dict.js` is OPTIONAL cleanup. INCOMPLETE plan — input-trio (PLANNER schema+parser+prompts) + nutrition_usda + 3 Task-7 deferrals remain (mostly → Aide migration; see PR #31 body). [[feedback_fast_pr_merges]]
**ROOT POINTER BUMP — PR #94 MERGED (`4fee929`):** root `heiberg-systems` develop now pins nourish at `e710d66`; planning docs reconciled (implementation-status row ❌→⚠️; roadmap Tasks 1-8 checklist corrected for drift, T1-5/7/8 done, T6 partial→Aide). Chore branch auto-deleted, develop synced. **CHAIN CLOSED.** Both repos tidy. The ONLY remaining multitenant-purity work is the Aide-deferred **input-trio** (PLANNER schema+parser+prompts) + `*_nutrition_usda` fields + 3 small Task-7 items — pick these up during the Nourish → Aide migration (the planner-response parser + nutrition_usda stay in Nourish regardless). Pre-existing untouched root dirty files remain: settings.local.json, scheduled_tasks.lock, 17peppertree.

**STATUS 2026-06-26 late:** 10 commits (my 4 + agent's 6; agent added `e227cc7`+`ec0f6cb` making the AI-generation OUTPUT side roster-dynamic: entry building, `person_analyses` nutrition audit, locked-day = all-members-compliant — tested vs a 3-person household). Full suite **456 pass / 1 fail** (same environmental subscriptions test). **Remaining = the INPUT TRIO** (tightly coupled, genuinely 2-person-shaped, needs an N=3 end-to-end generate-run integration harness before touching): `PLANNER_TOOL_SCHEMA` structural `Jako/Elize` keys ⇄ the inline planner-response parser in `generate_meal_plan_core` ⇄ the 2-person snack-floor filter (~L737-794) ⇄ gendered prompt prose in `prompts.py`/`chef.py`. Plus `nutrition_usda` aggregation fields (`usda._calculate_recipe_nutrition` fixed `{Jako,Elize}` return). Output side proven multi-tenant; input side still 2-person.

**VERIFIED (analyst.py):** the live analyst is `run_analyst_for_day` (pure LP/scipy) — correctly migrated (writes `ing["quantities"][person]` + dynamic `{person.lower()}_nutrition_usda`). The Claude-analyst block (`ANALYST_TOOL_SCHEMA`, `_build_day_analyst_system`, `_format_prior_adjustments`, `_apply_analyst_adjustments`→`_apply_adjustment_to_doc` which writes legacy `jako_quantity`, `FRIDAY_CHEAT_*`) is **DEAD leftover — zero call sites** (verified by grep). So analyst.py's ~47 hardcoded refs are NOT a live bug/gap; they're deletable cleanup. NO silent-drop bug in the live path. Remaining LIVE multi-tenancy gaps = the **input-trio** (PLANNER schema keys + planner-response parser + snack-floor filter ~L737-794 in core + gendered prose in prompts.py/chef.py) + `usda._calculate_recipe_nutrition` fixed `{Jako,Elize}` return. Aide-informed: planner SCHEMA+PROMPT belong in the future Aide agent (don't genericize in-place — throwaway); PARSER is coupled to the schema so defer with it; snack-floor + `_calculate_recipe_nutrition` are pure Nourish Layer-B (doable independently). Recommend: PAUSE the in-place input-trio (defer to Aide migration); optional low-risk cleanups = delete dead Claude-analyst block + dynamize `_calculate_recipe_nutrition`.

**EARLIER 2026-06-26 eve:** 8 commits on branch (my 4 + a background agent's 4 for Tasks 5-8). Full backend suite **453 pass / 1 fail** (the 1 = `test_subscriptions::...returns_503_when_unconfigured`, pre-existing/environmental — root `.env` sets `PAYSTACK_SECRET_KEY` so webhook returns 400 not 503; NOT caused by migration). auth.py + routers/ai_targets.py verified 100% cov. Branch NOT pushed, clean tree.

**⚠️ Migration is HALF done — the deterministic pipeline path is migrated; the AI/LLM path is NOT.** Done & coherent for the Jako/Elize dev household, but a different household would break in AI generation + nutrition aggregation. Remaining hardcoded refs (~270) concentrate in: `routers/ai/core.py` (68), `routers/ai/prompts.py` (66), `routers/pipeline/analyst.py` (47 — the **Claude analyst**: `FRIDAY_CHEAT_*` reserves, prompt still says "adjust jako_quantity/elize_quantity", `new_jako_quantity`/`new_elize_quantity` response parsing), `routers/ai/chef.py` (21), `nutrition_check.py` (18), `recipe_ops.py` (11), `recipes.py`/`weeks.py` (jako_multiplier/elize_multiplier), `usda.py` (10 — incl. `_calculate_recipe_nutrition` fixed `{"Jako","Elize"}` return + `*_nutrition_usda` fields). This is the deferred **deep Task 6** (planner schema structural keys + core.py parser + person_analyses threading + AI-analyst prompt/response) plus the **nutrition_usda** person-keyed nutrition fields (works for dev household, zeros for others). RISK: AI analyst writes `new_jako_quantity` to docs whose readers now prefer the `quantities` dict → adjustments may be silently ignored for migrated data — verify before relying on AI enrichment.

**Done (branch `feature/multitenant-purity`, in-place in the submodule, 8 commits, NOT pushed):**
- Task 1: `mongo_seed_recipes.json` → `quantities:{name:val}` dict + idempotent migration `app/database/migrations/mongo_001_quantities_dict.js`.
- Task 2: `routers/ai/models.py` `CHEF_TOOL_SCHEMA` → `quantities` object; `PLANNER_TOOL_SCHEMA` description genericized.
- Task 3: `routers/pipeline/chef.py` + `orchestrator.py` household-member-driven (quantities dict, member roster in system prompt).
- Task 4: `nutrition_math.py` reads `quantities` via new `_person_quantity` helper (case-insensitive + legacy `{person}_quantity` fallback); `scale_pipeline_day`/`build_analyst_hints` derive persons from `targets.keys()`. analyst.py deliberately untouched (legacy fallback keeps it working) — its de-hardcoding is Task 5. test_nutrition_math 35✓, pipeline cluster 142✓, nutrition_math coverage at baseline parity.
- Task 5 (`a7d266d`): deterministic analyst `run_analyst[_for_day]` dynamic `persons` roster; LP writes into `quantities`; `usda._calculate_recipe_nutrition` reads dict. (Did NOT touch the Claude-analyst prompt/response — see ⚠️ above.)
- Task 6 PARTIAL (`e3f3aae`): `recipe_ops._persist_enrichment` reads `quantities` (fixes the Task-2 broken path); `EnrichIngredient.quantities`; `NutritionalAnalysis.jako_analysis/elize_analysis` → `person_analyses: dict`. Deep planner schema/parser + ~40 literals DEFERRED.
- Task 7 (`a153560`): `ai_targets` `_get_calorie_floor` (85% of own target, env `CALORIE_FLOOR_MINIMUM`); `auth._build_bypass_user` env-driven (`DEV_BYPASS_USER_ID/_NAME/_EMAIL`); `weeks_views` snack pivot dynamic; `workouts` desc generic. weeks.py multipliers / workouts scoping / seed prose DEFERRED.
- Task 8 (`8ae5d7b`): frontend — generic metadata, insights defaults to session user, MealPlanClient dynamic per-member columns, dev-login generic. 265 FE tests ✓ 100% cov.

TDD throughout; verified via bind-mount runs (`docker compose --profile nourish run --rm --no-deps -v $(pwd)/backend:/app nourish-backend pytest ... --no-cov`). test_models 3✓, test_chef 13✓, pipeline cluster 31✓.

**Non-obvious decisions / gaps (carry forward):**
- **Do NOT open a PR / merge yet.** Tasks 1-3 are the *produce* side of the ingredient quantities-dict migration; the *consume* side (Task 4 `nutrition_math.py`, Task 5 `analyst.py`, **plus the un-tasked `usda.py:52` and the `jako_nutrition_usda`/`elize_nutrition_usda` doc fields**) still read `jako_quantity`. Merging now ships a broken pipeline. [[feedback_fast_pr_merges]] — Jako merges fast, so hold the PR until ≥ Tasks 4-5 land.
- **Live mongo migration deferred.** `mongo_001_quantities_dict.js` is committed but NOT applied to the shared `nourish` DB, and `seed_mongo.sh` reseed NOT run — doing so now breaks the running develop backend. Run at the data-format cutover after Tasks 4-5 + backend rebuild. Verified on a throwaway scratch DB only.
- **Live mongo DB is `nourish`** (`app/backend/mongo_db.py` default; root `.env` sets no `MONGO_DB`). `seed_mongo.sh`'s `meal_planning` default + `meal-planning-mongo` host are STALE (real host = `heiberg-mongodb-1`).
- **Planner structural person-keys deferred to Task 6.** Only the `PLANNER_TOOL_SCHEMA` *description* was changed; `_MEAL_SLOT`/snacks still key on `Jako`/`Elize` because the `core.py` parser that consumes them is Task 6 — change them together.
- Orchestrator reuses `list(targets.keys())` for the roster instead of the plan's redundant `_load_household_members` query (targets is already keyed by member name).
- planning-rules prose (`mongo_seed_planning_rules.json` rules `analyst_adjustment_priority`, `chef_single_serving_semantics`) still references `jako_quantity`/`elize_quantity` as a data-contract description — update alongside the AI-prompt/rules work (Task 6/7), not Task 1.
